using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// 태양을 중심으로 회전하고 싶다.
// 주기, 태양으로부터의 거리
public class KJH_Planet : MonoBehaviour
{
    public Transform sunPos;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
